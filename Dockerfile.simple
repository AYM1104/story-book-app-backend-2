# Python 3.12の公式イメージを使用
FROM python:3.12-slim

# 作業ディレクトリを設定
WORKDIR /app

# Pythonの依存関係をコピーしてインストール（最小限）
COPY requirements_simple.txt .
RUN pip install --no-cache-dir -r requirements_simple.txt

# アプリケーションコードをコピー
COPY app/main_simple.py ./app/

# ポート8080を公開（Cloud Runのデフォルトポート）
EXPOSE 8080

# 環境変数を設定
ENV PORT=8080
ENV HOST=0.0.0.0

# Cloud Runでアプリケーションを起動
CMD exec uvicorn app.main_simple:app --host $HOST --port $PORT --workers 1
